export type Recipe = {
  id: string;
  userID: string;
  title: string;
  ingredients: string;
  estimate: string;
  cuisine: string;
  result_img: string;
  time: string //The time the recipe was posted
};